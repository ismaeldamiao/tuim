name: Build and test
on: [push, pull_request]

jobs:
  xnu:
    runs-on: macos-latest
    strategy:
      fail-fast: false
      matrix:
        c_compiler: [gcc, clang]
    steps:
      - uses: actions/checkout@v4
      - name: Build, Install and Test
        shell: sh
        working-directory: ${{ github.workspace }}
        run: |
          . scripts/build.sh "/tmp" aarch64 xnu
